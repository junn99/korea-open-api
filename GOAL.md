# 목표 (Goal)

한국에서 제공되는 **무료 오픈 API**를 체계적으로 조사하여, 구조화된 카탈로그로 정리한다.

## "오픈 API" 정의 (이 프로젝트 기준)

- **포함**: 요금이 무료인 API (완전 무료, 또는 "무료 구간(free tier)이 존재"하는 경우 포함)
- **포함**: 회원가입 / 키 발급 승인 / 공동인증서 등 절차가 필요해도 비용이 무료면 포함
- **제외**: 호출당 과금되거나 무료 구간이 전혀 없는 순수 상용 API
- **제외**: 더 이상 운영되지 않는(deprecated) API — 단, 발견 시 별도 표시

## 조사 전략 (하이브리드 A + B)

### A. 전수형 — 공식 카탈로그가 있는 출처 (자동 수집으로 최대한 완전하게)
분모가 정의되는 출처이므로 "거의 전수"를 목표로 한다.
- **공공데이터포털 (data.go.kr)** — 공식 목록 조회 OpenAPI로 전수 수집 시도
- **서울 열린데이터광장 (data.seoul.go.kr)** 등 주요 지자체 포털
- **네이버 개발자 센터 (NAVER Developers)** — 공개 API 목록
- **카카오 개발자 (Kakao Developers)** — 공개 API 목록
- **공공기관/부처별 OpenAPI 포털** (기상청, 통계청 SGIS, 국토부 VWorld 등)

### B. 큐레이션형 — 카탈로그가 없는 민간/분야별 주요 API
집계처가 없는 영역은 분야별로 실제 많이 쓰는 핵심 무료 API를 직접 큐레이션한다.
- 금융: 오픈뱅킹(금융결제원), 마이데이터, 한국은행 ECOS, 금융위/금감원 등
- 지도/교통: VWorld, 대중교통 API, 국가교통DB 등
- 날씨/환경: 기상청 단기예보, 에어코리아 등
- 생활/검색: 네이버 검색, 카카오 로컬, 우정사업본부 우편번호 등
- 기타 스타트업·기업의 공개 무료 API (발견되는 대로 추가)

## 각 API별 수집 항목

| 항목 | 설명 |
|------|------|
| `name` | API 이름 |
| `provider` | 제공기관 |
| `category` | 공공 / 금융 / 지도 / 교통 / 날씨·환경 / 생활 / 검색 / 기타 |
| `description` | 한 줄 요약 |
| `docs_url` | 공식 문서 URL (필수) |
| `auth` | 인증 방식 (API Key / OAuth / 공동인증서 / 없음) |
| `pricing` | free / free-tier |
| `signup_required` | 회원가입·승인 필요 여부 (true/false) |
| `format` | 응답 포맷 (JSON / XML / REST / SOAP) |
| `rate_limit` | 호출 제한 (확인 가능한 경우) |
| `source` | 어느 출처에서 수집했는지 |
| `note` | 비고. 미확인 정보는 "(미확인)" 표기 |

## 산출물

- `data/korea-open-apis.json` — 위 스키마로 구조화된 데이터 (일관성 유지)
- `README.md` — 카테고리별 표 형태의 사람이 읽기 좋은 카탈로그
- 요약 통계 — 총 개수, 카테고리별 분포, free vs free-tier 비율

## 작업 규칙

- 출처 URL을 반드시 기록한다. 추측·미확인 정보는 `(미확인)`으로 표시한다.
- 존재하지 않는 API를 지어내지 않는다. 확인된 것만 기록한다.
- 한 번에 끝내려 하지 말고 **출처별로 나눠 점진적으로** 카탈로그를 채우고 단계별로 커밋한다.
- 중복 API는 병합하고, 폐기된 API는 표시한다.

## 완료 기준 (Definition of Done)

- A 출처(공공데이터포털 등 카탈로그 보유처)는 전수에 가깝게 수집 완료
- B 영역은 분야별 주요 무료 API가 빠짐없이 큐레이션됨
- `data/korea-open-apis.json` 과 `README.md` 가 서로 일치
- 모든 항목에 최소 `name`, `provider`, `docs_url`, `auth`, `pricing` 이 채워짐

## 한계 (정직한 명시)

- 카탈로그가 없는 민간 무료 API는 **"전수"가 아니라 "대표 큐레이션"** 이다.
- API 목록은 시점 기준 스냅샷이며, 신규 추가·폐기로 인해 시간이 지나면 낡는다.
